apiVersion: v1
kind: Service
metadata:
  name: {{ include "kltestchart.fullname" . }}
  labels:
    {{- include "kltestchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kltestchart.selectorLabels" . | nindent 4 }}
